version: '3.8'

services:
  api:
    build: .
    container_name: kmc-api
    environment:
      ASPNETCORE_ENVIRONMENT: Production
      MongoDbSettings__ConnectionString: mongodb+srv://awantha:ABdgpqSthXmK1k8K@cluster0wolfenox.aza9jrr.mongodb.net/newone
      MongoDbSettings__DatabaseName: newone
    ports:
      - "80:80"
      - "443:443"
    networks:
      - kmc-network
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost/health"]
      interval: 30s
      timeout: 3s
      retries: 3

networks:
  kmc-network:
    driver: bridge
